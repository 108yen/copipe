import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#eceff1',
            light: '#ffffff',
            dark: '#babdbe',
        }
    }
});

export default theme;