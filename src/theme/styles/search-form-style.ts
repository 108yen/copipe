import { CSSUIObject } from "@yamada-ui/react"

export const searchFormStyle: Record<string, CSSUIObject> = {
  button: {
    fontWeight: "normal",
    color: "gray",
    size: "xs",
    w: "fit-content",
  },
}
